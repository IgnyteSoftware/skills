# [Observable change]

## Outcome
Current behavior and expected behavior. Link the accepted decision or selected
prototype when relevant. For a bug, give the reproduction and affected environment.

## Scope
Required change and material exclusions. Reference existing project conventions.

## Acceptance
- [Observable behavior that proves the requested outcome]

## Dependencies
Link real blockers, or omit this section. Add the parent outcome/milestone if useful.

## Verification
What evidence will establish acceptance? Use the repository's existing build, run,
format, lint, and test instructions. Record task-specific checks here. Authoring new
tests follows the user's request and repository policy; it is separate from running
existing tests. For deployment, identify the target, artifact, and recovery approach.

## Delivery
Link the approved workflow profile and name the requested endpoint: local review
package, local commit, published branch, PR readiness, or explicitly authorized
release/deployment. Use only endpoints that the profile permits. Name a human
checkpoint when required; do not assume a PR or infer authority from this template.
